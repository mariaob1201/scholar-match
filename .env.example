# Copy to .env and fill in.

# Required for the LLM match-explanation step (scholar_match/explain.py).
ANTHROPIC_API_KEY=sk-ant-...

# Recommended by OpenAlex: putting your email in requests gets you into the
# faster "polite pool". No account or key needed.
OPENALEX_MAILTO=you@wisc.edu
